version: '3.7'

services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - "1111:8080"  # Cambia el puerto de Jenkins web interface a 1111
      - "50000:50000"  # Jenkins agent port
    volumes:
      - jenkins-data:/var/jenkins_home

volumes:
  jenkins-data:
